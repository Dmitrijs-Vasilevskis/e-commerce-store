export interface UserInterface {
    id: number;
    email: string;
    username: string;
    token: string;
}

export interface CustomerInterface {
    id: number;
    email: string;
    firstName: string;
    lastName: string;
}