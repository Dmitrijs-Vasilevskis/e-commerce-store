import { DynamicRendererDirective } from './dynamic-renderer.directive';

describe('DynamicRendererDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicRendererDirective();
    expect(directive).toBeTruthy();
  });
});
